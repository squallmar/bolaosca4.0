// Arquivo legado não usado mais. Mantido apenas para evitar import quebrado.
// Autenticação agora usa cookie httpOnly e contexto em 'src/authContext.js'.
export function AuthProvider({ children }) { return children; }
export function useAuth() { return {}; }
export default {};