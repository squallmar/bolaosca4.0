# Server
PORT=3001
BASE_URL=http://localhost:3001

# Database (PostgreSQL)
# Formato: postgres://usuario:senha@host:porta/banco
DATABASE_URL=postgres://postgres:postgres@localhost:5432/bolao

# CORS
CORS_ORIGIN=http://localhost:3000

# Auth (se houver JWT em outros arquivos)
JWT_SECRET=defina-um-segredo-se-necessario

# SMTP (para envio de emails do Anuncie)
SMTP_HOST=smtp.seu-provedor.com
SMTP_PORT=587
SMTP_USER=seu-usuario
SMTP_PASS=sua-senha
MAIL_FROM="Bolao SCA <no-reply@seu-dominio.com>"
MAIL_TO=destinatario@seu-email.com
