# Porta interna do backend (túnel aponta para esta)
PORT=3001
PORT=8080
NODE_ENV=production
DISABLE_HSTS=true  # enquanto estiver só em HTTP (LAN / sem certificado) mantenha true; remova quando tiver HTTPS

# Banco (ajuste usuário, senha, host, porta, base)
DATABASE_URL=postgres://usuario:mM202038@localhost:5432/bolao_db

# Segurança
# Gere algo forte (>=64 bytes base64). Exemplo (não use este em produção):
# JWT_SECRET=Kk7w2c7f4tQ0pVJ6fWcVYcTzC8tYF3nPq9Lx2fH0mYdS5rV8qBz1sD6hJ0lN3pQ=
JWT_SECRET=+tkVZyUK6v+PyjVZKAe3lXxEYsvogo16sMl0tZQLph5uBp9EQaxa9aUtXmb04//abAlBhOJQY0Ixgu3mRyWZFg==
BCRYPT_COST=12
IMAGE_STRICT=true

# Origens permitidas (subdomínios do túnel)
# Origens permitidas (inclui domínios + localhost + IP da LAN para testes internos)
# Adicione aqui depois o IP público quando fizer o port forwarding (ex: http://SEU_IP_PUBLICO:3001 ou http://SEU_IP_PUBLICO)
 CORS_ORIGIN=https://app.bolaosca.com,https://api.bolaosca.com,http://localhost:3001,http://127.0.0.1:3001,http://192.168.1.22:3001,http://192.168.1.14:3001,http://192.168.1.14:3002,http://177.131.189.98,http://177.131.189.98:3001,http://bolaosca.sytes.net,http://bolaosca.sytes.net:8080,http://192.168.1.12,http://192.168.1.14,http://192.168.0.100

# Base usada para montar URLs de upload / avatar
# BASE_URL temporária apontando para o IP da sua LAN (gera URLs corretas para avatars/escudos ao acessar pela rede interna)
# Quando expor para fora sem domínio, pode usar o IP público aqui. Quando tiver domínio definitivo volte a trocar.
# Se o port forwarding estiver mapeando 80 -> 3001 use sem porta; se ainda acessa diretamente :3001 pode ajustar temporariamente.
BASE_URL=http://bolaosca.sytes.net:8080

# E-mail (preencha se for usar envio; senão pode deixar vazio que o código desativa)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=no-reply@bolaosca.com
MAIL_TO=contato@bolaosca.com
MAIL_PROVIDER=

# (Opcional) Política / contato segurança
SECURITY_POLICY_URL=https://app.bolaosca.com/security

# Ajustes de cache/TTL podem ser adicionados futuramente:
# CLASSIFICACAO_TTL_MS=60000

# ================= EMAIL (ATUAL: MAILTRAP SANDBOX) =================
MAIL_PROVIDER=mailtrap
MAIL_FROM=bolao@test.local
MAIL_TO=seuemail@gmail.com

# Credenciais Mailtrap
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=5db4c526f608dc
SMTP_PASS=5cf72ae833ade4

# ================= (OPCIONAL) GMAIL (App Password) =================
# MAIL_PROVIDER=
# MAIL_FROM=bolaosca@gmail.com
# MAIL_TO=bolaosca@gmail.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=bolaosca@gmail.com
# SMTP_PASS=nklpepvfwvbnwsmd

# Observação: após editar este arquivo reinicie o backend para aplicar mudanças.

# Segurança: mantenha .env no .gitignore
# Se precisar rotacionar o JWT_SECRET basta trocar o valor e invalidar logins antigos.
